# @pinpatch/overlay

Injected browser overlay bundle used by `pinpatch dev` for pin placement, request capture, and task status UX.

**Status:** Internal package (runtime dependency of `pinpatch`, not intended for direct end-user installation).
