# @pinpatch/ui

Shared React UI primitives and styles used by Pinpatch applications (overlay and fixtures).

**Status:** Internal package (published for workspace/runtime dependencies, not a supported standalone end-user package).
