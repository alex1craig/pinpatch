# @pinpatch/proxy

Reverse proxy package for Pinpatch that injects the overlay script into HTML responses and forwards WebSocket traffic.

**Status:** Internal package (not intended for direct end-user installation).
